primer: en moviesController creamos un try Catch(bloque), en el cual desarrollamos una funcion donde llamamos al aUrl de la Api que vamos a utilzar para traer los datos.
segundo: creo la const para que me traiga los datos como un Json, despues rendenrizo la vista para que me desarme ese Json y me liste segun los resultados.
